import { config } from "@vue/test-utils";
import { vi } from "vitest";

// Global test setup
config.global.stubs = {};

// Mock any global properties or plugins if needed
// Example: config.global.plugins = [createTestingPinia()];
